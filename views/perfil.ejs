<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f7fa;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #e0e0e0;
        }
        
        .header h1 {
            color: #333;
            margin: 0;
        }
        
        .back-btn {
            background: #6c757d;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
        }
        
        .profile-card {
            background: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            margin-bottom: 30px;
        }
        
        .profile-header {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
        }
        
        .avatar {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 32px;
            font-weight: bold;
            margin-right: 20px;
        }
        
        .profile-info h2 {
            margin: 0 0 5px 0;
            color: #333;
        }
        
        .profile-info p {
            margin: 5px 0;
            color: #666;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        
        .info-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        
        .info-label {
            font-weight: bold;
            color: #555;
            display: block;
            margin-bottom: 5px;
        }
        
        .info-value {
            color: #333;
        }
        
        .badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            margin-left: 10px;
        }
        
        .badge-admin {
            background: #dc3545;
            color: white;
        }
        
        .badge-super {
            background: #007bff;
            color: white;
        }
        
        .badge-gestao {
            background: #28a745;
            color: white;
        }
        
        .badge-comum {
            background: #6c757d;
            color: white;
        }
        
        .actions {
            display: flex;
            gap: 10px;
            margin-top: 30px;
        }
        
        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }
        
        .btn-primary {
            background: #007bff;
            color: white;
        }
        
        .btn-danger {
            background: #dc3545;
            color: white;
        }
        
        .escolas-list {
            margin-top: 10px;
        }
        
        .escola-tag {
            display: inline-block;
            background: #e3f2fd;
            color: #1976d2;
            padding: 5px 10px;
            margin: 5px 5px 0 0;
            border-radius: 15px;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>👤 Meu Perfil</h1>
            <a href="/dashboard" class="back-btn">← Voltar ao Dashboard</a>
        </div>
        
        <div class="profile-card">
            <div class="profile-header">
                <div class="avatar">
                    <%= user.nome.charAt(0).toUpperCase() %>
                </div>
                <div class="profile-info">
                    <h2><%= user.nome %></h2>
                    <p><%= user.email %></p>
                    <p>
                        Tipo: <strong><%= user.tipo.toUpperCase() %></strong>
                        <span class="badge badge-<%= user.tipo %>">
                            <%= user.tipo === 'administrador' ? 'ADMIN' : 
                               user.tipo === 'supervisao' ? 'SUPERVISÃO' :
                               user.tipo === 'gestao' ? 'GESTÃO' : 'COMUM' %>
                        </span>
                    </p>
                </div>
            </div>
            
            <div class="info-grid">
                <div class="info-item">
                    <span class="info-label">Status</span>
                    <span class="info-value">
                        <% if (user.ativo) { %>
                            <span style="color: green;">● Ativo</span>
                        <% } else { %>
                            <span style="color: orange;">● Aguardando aprovação</span>
                        <% } %>
                    </span>
                </div>
                
                <div class="info-item">
                    <span class="info-label">Primeiro Acesso</span>
                    <span class="info-value">
                        <%= user.primeiroAcesso ? 'Sim - Troque sua senha' : 'Não' %>
                    </span>
                </div>
                
                <% if (user.departamento) { %>
                <div class="info-item">
                    <span class="info-label">Departamento</span>
                    <span class="info-value">
                        <%= user.departamento === 'pedagogico' ? 'Pedagógico' : 'Secretaria' %>
                    </span>
                </div>
                <% } %>
                
                <div class="info-item">
                    <span class="info-label">Data de Cadastro</span>
                    <span class="info-value">
                        <%= new Date(user.criadoEm).toLocaleDateString('pt-BR') %>
                    </span>
                </div>
            </div>
            
            <% if (user.escolas && user.escolas.length > 0) { %>
            <div class="info-item" style="grid-column: 1 / -1;">
                <span class="info-label">Escolas Vinculadas</span>
                <div class="escolas-list">
                    <% user.escolas.forEach(function(escola) { %>
                        <span class="escola-tag"><%= escola %></span>
                    <% }); %>
                </div>
            </div>
            <% } %>
            
            <div class="actions">
                <button class="btn btn-primary" onclick="alert('Funcionalidade em desenvolvimento!')">
                    ✏️ Editar Perfil
                </button>
                <button class="btn btn-primary" onclick="alert('Funcionalidade em desenvolvimento!')">
                    🔒 Alterar Senha
                </button>
                <button class="btn btn-danger" onclick="window.location.href='/'">
                    ↩️ Sair
                </button>
            </div>
        </div>
        
        <div style="text-align: center; color: #666; font-size: 14px; margin-top: 30px;">
            <p>Sistema de Demandas Escolares | Versão 1.0.0</p>
            <p>Usuário: <%= user.email %> | Tipo: <%= user.tipo %></p>
        </div>
    </div>
    
    <script>
        // Adicionar classes CSS para badges
        document.addEventListener('DOMContentLoaded', function() {
            const badge = document.querySelector('.badge');
            if (badge) {
                const tipo = '<%= user.tipo %>';
                badge.classList.add('badge-' + tipo);
            }
        });
    </script>
</body>
</html>